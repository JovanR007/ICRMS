@model IEnumerable<FinalCapstone.Program>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<div class="card card-cascade narrower">

   
    <div class="view view-cascade gradient-card-header blue-gradient narrower py-2 mx-4 mb-3 d-flex justify-content-between align-items-center">
        <div></div>
        <a class="white-text mx-3">List of Programs</a>
        <div></div>
    </div>
    
    <div class="px-4">
        <div class="table-wrapper">
            <table class="table table-hover mb-0">    
                <thead>
                    <tr>                    
                        <th class="th-lg">
                            <a>
                               Program
                                <i class="fas fa-sort ml-1"></i>
                            </a>
                        </th>    
                      
                </thead>               
                <tbody>
                    @foreach (var item in Model)
                    {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.program1)
                        </td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td>
                            <button onclick="location.href='@Url.Action("Edit","Programs", new { id =item.program_id})'">Edit</button>
                            <button onclick="location.href='@Url.Action("Delete","Programs", new { id =item.program_id})'">Delete</button>
                        </td>
                    </tr>


                    }
                </tbody>
            </table>         
        </div>
    </div>
</div>
